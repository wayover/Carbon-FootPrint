@model IEnumerable<WebApplication1.Models.Wspolczynnik>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Wspolczynnik_Nazwa.Nazwa)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Zrodlo.Doi)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Wartosc)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Niepewnosc)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Wspolczynnik_Nazwa.Nazwa)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Zrodlo.Doi)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Wartosc)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Niepewnosc)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
